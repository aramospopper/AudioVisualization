import React from 'react';

// ChartTwo: removed (legacy). Stubbed to avoid bundling unused chart code.
// Keeps the original export so pages that import it won't break.
const ChartTwo: React.FC = () => null;
export default ChartTwo;

